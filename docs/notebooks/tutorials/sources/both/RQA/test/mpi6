#!/bin/bash
#SBATCH --job-name=mpi6_bn
#SBATCH --nodes=4
#SBATCH --ntasks-per-node=96       # MPI ranks per node (1 core per rank)
#SBATCH --mem-per-cpu=6G
#SBATCH --time=02:30:00
#SBATCH --output=mpi6.%j.out
#SBATCH --error=mpi6.%j.err
#SBATCH --mail-type=BEGIN,END,FAIL         # Notifications for job done & fail
#SBATCH --mail-user=fcastro@princeton.edu

# ---- Load modules ----
module purge
module load anaconda3/2025.6
module load openmpi/gcc/4.1.6

# ---- Activate environment ----
source activate desc-ray

# ---- CPU-only configuration ----
#export OMP_NUM_THREADS=1
#export MKL_NUM_THREADS=1
export OMP_NUM_THREADS=$SLURM_CPUS_PER_TASK
export MKL_NUM_THREADS=$SLURM_CPUS_PER_TASK
export JAX_PLATFORMS=cpu

# ---- Python script to run ----
PYTHON_SCRIPT="mpi6.py"    # replace with your notebook-converted Python script

# ---- Run MPI job ----
if [ -f "$PYTHON_SCRIPT" ]; then
    echo "Running Python script $PYTHON_SCRIPT with $SLURM_NTASKS MPI ranks..."
    mpiexec -n $SLURM_NTASKS python "$PYTHON_SCRIPT"
else
    echo "Error: Python script $PYTHON_SCRIPT not found."
fi

echo "Job finished."
