# standard install requirements
-r ../requirements.txt

# building the docs
nbsphinx > 0.8.5
sphinx > 3.0.0
sphinx-argparse
sphinx_copybutton
sphinx-rtd-theme

# linting
black == 21.7b0
click == 8.0.4
flake8 >= 4.0.1
pylint >= 2.12.2

# testing and benchmarking
codecov
markupsafe == 2.0.1
nbmake
pytest >= 5.0.0
pytest-benchmark
pytest-cov >= 2.6.0
pytest-mpl == 0.16.1
shapely >= 1.8.2
pytest-monitor
pytest-split

# building
build
